// import { getRepository } from 'typeorm';

export class RepositoryService {

	// public async saveIfNotExists<E>(entity: new () => E, row: E, name: string) {

	// 	const measurementUnitFound = await entity.findOne({ name });

	// 	if (!measurementUnitFound) {

	// 		return getRepository(entity).save({ name }).then(measurementUnit => measurementUnit.id);
	// 	}

	// 	return measurementUnitFound.id;
	// }
}